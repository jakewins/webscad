These are things we need to support.

  MODULES:

    Primitives:
      cube
      cylinder
      sphere
      polyhedron
      square
      circle
      polygon

    CSG ops:
      union
      difference
      intersection
	    render // Should not be needed

    Transform ops:
	    scale
	    rotate
	    mirror
	    translate
	    multmatrix
	    color
	    minkovski
	    hull
	    
	  Control:
	    child
	    echo
	    assign
	    for
	    intersection_for
	    if // This is not a module
	    
	  Advanced:
	    glide
	    subdiv
	
    Other:
      surface
	    projection
      import_dxf
      import_stl
      import_off
      dxf_linear_extrude
      linear_extrude (same as above)
      dxf_rotate_extrude
      rotate_extrude (same as above)
      dxf_dim
      dxf_cross
      
  FUNCTIONS:
    abs
    sign
	  rands
	  min
	  max
	  sin
	  cos
	  asin
	  acos
	  tan
	  atan
	  atan2
	  round
	  ceil
	  floor
	  pow
	  sqrt
	  exp
	  log
	  ln
	  str
	  lookup
